import { ActiveWindow } from '@/shared/types/activeWindow';

interface Props {
  windows: ActiveWindow.Result[];
}
const ChartContainer: React.FC<Props> = ({ windows }) => {
  <div>
    <div>
      <h2>Total time:</h2>
    </div>
  </div>;
};
